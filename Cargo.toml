[profile.release]
panic = 'unwind'

[workspace]
members = ['node', 'pallets/*', 'runtime', 'runtime/types', 'tools/*']
resolver = "2"

[profile.production]
inherits = "release"
lto = true
codegen-units = 1

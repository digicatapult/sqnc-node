[profile.release]
panic = 'unwind'

[workspace]
members = ['node', 'pallets/*', 'runtime']

[dscp-node.metadata.cross.target.aarch64-unknown-linux-gnu]
xargo = false
dockerfile =  "./docker/cross.dockerfile"
